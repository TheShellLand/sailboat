#!/bin/sh
# SmartOS script for listing VMs
#
# Prints a list of VMs including its nic address
#
#
# alpha0.2
# 0xB4AE7C4C Eric Jaw
#

vmadm list -o uuid,type,ram,state,nics.0.ip,hostname,alias -s alias
